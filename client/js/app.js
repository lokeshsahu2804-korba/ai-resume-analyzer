/**
 * AI Resume Analyzer — App Initialization
 * Phase 1: Confirms the frontend JavaScript is loading correctly.
 */

document.addEventListener('DOMContentLoaded', () => {
  console.log('AI Resume Analyzer — App initialized');
  console.log(`API Base URL: ${API_BASE_URL}`);
});
